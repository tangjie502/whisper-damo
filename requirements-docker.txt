# Docker 专用依赖（精简直接依赖，避免跨平台哈希冲突）
# 不锁定所有传递依赖，由 pip 在 Linux 环境自动解析正确的 wheel

# Web 框架
Flask>=3.1

# Whisper 转写引擎
faster-whisper>=1.2

# SenseVoice 引擎（可选，约 500 MB 模型）
funasr>=1.3
librosa>=0.11

# 生产 WSGI 服务器
gunicorn>=22
